import 'dart:ui';

class AppColors {
  static const backgroundLight = Color(0xFFFFFFFF);
  static const foregroundLight = Color(
    0xFF252525,
  ); // approximation du oklch(0.145 0 0)
  static const cardLight = Color(0xFFFFFFFF);
  static const primaryLight = Color(0xFF030213);

  static const backgroundDark = Color(0xFF252525);
  static const foregroundDark = Color(0xFFFFFFFF);
  static const cardDark = Color(0xFF252525);
  static const primaryDark = Color(0xFFE0E0E0);
}
